3x + 4 = y 
7x - 13 = y
5x + 9 = y


m1x + c1 = y1
m2x + c2 = y2
m3x + c3 = y3


m2 < m5 == m4 <  m1 <= m3

m1 < m2 < m3 // slope non decresing
m1 > m2 > m3 // slope non increasing

m1 <= m2 >= m3

lo + (hi - lo) / 3
hi - (hi - lo) / 3

mn = min(parents, node)


6, 9, -1, 4, -10, -25

p = {-25, -10, -1, 4, 6, 9}


mid;
p[mid];

def val(line, mid)
def val(line, p[mid])
{
	return m * x + c; 
}

def val()
{
	return a * x * x + b * x + c;	
}

y = mx + c
y = ax^2 + bx + c;

struct line
{
	ll a, b, c;	
}