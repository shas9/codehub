#include <bits/stdc++.h>

#define scand(value) scanf("%d", &value)
#define printd(value) printf("%d", value)

#define scanlld(longvalue) scanf("%lld", &longvalue)
#define printlld(longvalue) printf("%lld", longvalue)

#define scanc(letter) scanf("%c", &letter)
#define printc(letter) printf("%c", letter)

#define scans(name) scanf("%s", name)
#define prints(name) printf("%s", name)

#define printnewline printf("\n")

#define TCase(x) for(long long int indexing = 1; indexing <= x; indexing++)

#define ll long long

#define printcase(ans) printf("Case %lld: %lld\n", indexing, ans)

using namespace std;

int main()
{
    ll size;
    ll i;
    string pic;

    scanlld(size);

    TCase(size)
    {
        //printlld(size);
        printcase(size);
        printnewline;
    }
    //printlld(i);


}
