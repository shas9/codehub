#include <bits/stdc++.h>

using namespace std;

int main()
{
    int testcase;
    int i, j, k, l;

    //string input;

    int size;

    cin >> testcase;

    int countS;
    int countR;

    int pos;
    int diff;

    int max;
    int flag;

    vector < int > input;
    vector < int > sorted;
    vector < int > temp;

    for(i = 1; i <= testcase; i++)
    {
        input.clear();
        sorted.clear();
        countR = 0;
        countS = 0;

        pos = 0;
        flag = 0;
        max  = 0;

        cin >> size;

        for(j = 0; j < size; j++)
        {
            cin >> k;
            input.push_back( k );
        }

        flag = 0;
        6666666+

        while(size.input())
        {
            sorted = input;
            max = sorted(size.input() - 1);

            for
        }
    }
