#include <bits/stdc++.h>

using namespace std;

int main()
{
    char a = '0';
    printf("%d", a);
}

/*int main()
{
    vector < int > sorted;
    int max1;
    int max2;
    int max3;

    int length;

    while(1)
    {
        cin >> max1;
        if(max1 == 0) break;
        sorted.push_back( max1 );
    }
    sort(sorted.begin(), sorted.end());

		length = sorted.size();

		max1 = sorted[length - 1];
		max2 = sorted[length - 2];
		max3 = sorted[length - 3];

		cout << max1 << endl;
		cout << max2 << endl;
		cout << max3 << endl;
}*/
