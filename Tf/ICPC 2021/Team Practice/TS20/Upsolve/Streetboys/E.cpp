#include<bits/stdc++.h>

using namespace std;

int main()
{
    int s;
    cin >> s

    while(s > 0)
    {
        int lastdigit = s % 10;
        s /= 10;

        // if(s <= 9) if(s == 9) no invert;
    }

    cout << s << "\n";
}

