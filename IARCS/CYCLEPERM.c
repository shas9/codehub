#include <stdio.h>

int main()
{
    int size;

    scanf("%d", &size);

    int a[size];

    int index;
    int pos, num, pos1;

    index = pos - 1;

    int i;

    for(i = 0; i < size; i++)
    {
        scanf("%d", &a[i]);
    }

    pos = 1;

    pos1 = pos;
    while(pos != num)
    {
        num = x[pos1];
        pos1 = x[num - 1];

        printf("")
    }
}
